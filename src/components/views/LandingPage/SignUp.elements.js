import styled from "styled-components";

import { Wrapper } from "../../reusable/Wrapper.elements";

export const SignUp = styled(Wrapper)`
  padding-bottom: 2rem;
  font-size: 2.5rem;
  text-align: center;
  .signup-cta__link {
    text-decoration: underline;
    font-size: 2.5rem;
  }
`;
